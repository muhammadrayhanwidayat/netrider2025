



```
Which TCP flag is responsible for resetting the connection when an error or timeout occurs?
```
RST

```
Which of following is a valid IPv6 unique local address? Maximum Attempts : 1 Select one from the below choices: 
FECE:1234::1 
FEBE:ACAD::1 
2023:FD25::1/64 FD12:3456:789a::/64
```
FD12:3456:789a::/64

```
Which subnet would include the IPv4 address 198.133.219.126 as a maximum usable host address in this subnet? Maximum Attempts : 1 Select one from the below choices: 198.133.219.32/27 198.133.219.80/28 198.133.219.64/26 198.133.219.128/25
```
198.133.219.64/26
```
Which of following terminology is a more accurate representation of the effective data transfer rate? Maximum Attempts : 1 Select one from the below choices: 
Throughput 
Goodput 
Bandwidth 
Output
```
Goodput
```
Given a /48 Global Routing Prefix and a /64 prefix, what is the subnet portion of the following address: 2023:db8:acad:2727:1111:3333:5555:2025? Maximum Attempts : 1 Select one from the below choices: 
2727 
acad 
1111 
3333
```
2727

```
Which of the following is a valid IPv6 global unicast address? Maximum Attempts : 1 Select one from the below choices: 2021:db9::5678:1::100/64 FC00:acad:ABCD:DEF:1234:5678:0:1/64 
2023:5678:ACAD:1111:2222::100/64 2023::230:F2FF:FEG7:4E01/64
```

2023:5678:ACAD:1111:2222::100/64

```
Which of the following statements about IPv6 address resolution is correct?

Maximum Attempts : 1

Select one from the below choices:
ARP Requests and ARP Replies are used for IPv6 address resolution

ICMPv6 Router Solicitation and Router Advertisement messages are used for IPv6 address resolution

IPv6 has a different address format than IPv4, so IPv6 does not require the address resolution process

ICMPv6 Neighbor Solicitation and Neighbor Advertisement messages are used for MAC address resolution
```
ICMPv6 Neighbor Solicitation and Neighbor Advertisement messages are used for MAC address resolution

```
Which of the following ICMP messages is for both IPv4 and IPv6?

Maximum Attempts : 1

Select one from the below choices:
Neighbor Solicitation message
Time exceeded
Neighbor Advertisement message
Traceroute unreachable

```
Time exceeded
```
Amy, a staff member at ABC Inc., out of curiosity, she use tools to determine which ports are open or closed on a server of her company's business rival. What type of attack is described in this scenario?

Maximum Attempts : 1

Select one from the below choices:
Malware attack
Access attack
Reconnaissance attack
Denial of service attack

```

Reconnaissance attack

```
Which two of the following are Access Attacks(Choose two)?

Maximum Attempts : 1

Select all that apply from the below choices:
Trust Exploitation
Internet Queries
Denial of service
Man-in-the-Middle

```
Trust Exploitation
Man-in-the-Middle

```
What happens when a directly connected static route is configured with Ethernet interface?

Maximum Attempts : 1

Select one from the below choices:
A static route with the source address of a data packet as the next hop IP address will be added into routing table

A static route with the destination address of a data packet as the next hop IP address will be added into routing table

As the static route points to an Ethernet interface and has no next hop information, the router performs ARP on the Ethernet for every destination

The router add this static route as a default route

```

As the static route points to an Ethernet interface and has no next hop information, the router performs ARP on the Ethernet for every destination

```
Which of the following descriptions about stateless DHCPv6 is true?

Maximum Attempts : 1

Select one from the below choices:
The stateless DHCPv6 server provides information that is identical for all devices on the network such as the IPv6 address of a DHCPv6 server

The ipv6 nd other-config-flag interface configuration command will enable stateless DHCPv6

The ipv6 nd managed-config-flag interface configuration command will enable stateless DHCPv6

The stateless DHCPv6 server is providing the DHCPv6 server address for IPv6 clients

```
The ipv6 nd other-config-flag interface configuration command will enable stateless DHCPv6

```
In which of the following cases the Controller-based APs are commonly used?

Maximum Attempts : 1

Select one from the below choices:
In cases where many APs are required in the network
In cases where the APs can only support 2.4GHz
In cases where only a couple of APs are required in the network
In cases where the APs can only support 5GHz
```
In cases where many APs are required in the network

```
Which of the following is a correct description of STP attacks?

Maximum Attempts : 1

Select one from the below choices:
Enable BPDU guard on ports attached to next level switches in order to mitigate STP Attacks

STP attacks can cause sub-optimal route path or even routing loops

Enable BPDU guard on ports attached to end devices in order to mitigate STP Attacks

Enable PortFast on ports attached to switches in order to mitigate STP Attacks
```

Enable BPDU guard on ports attached to end devices in order to mitigate STP Attacks

```
What is Supplicant in IEEE 802.1X?

Maximum Attempts : 1

Select one from the below choices:
The Supplicant acts as an intermediary between the client and the network
Supplicant validates the identity of the client and notifies the switch that the client is or is not authorized to access the LAN and switch services
The Supplicant is a device to audit what actions the users performed while accessing the networks
Supplicant is a device running 802.1X-compliant client software, which is available for wired or wireless devices
```

Supplicant is a device running 802.1X-compliant client software, which is available for wired or wireless devices

```
Why is NAT not needed in IPv6?

Maximum Attempts: 1

Select one from the below choices:
The end-to-end connectivity problems that are caused by NAT are solved because the number of routes increases with the number of nodes that are connected to the Internet.

Because IPv6 has integrated security, there is no need to hide the IPv6 addresses of internal networks.​

The problems that are induced by NAT applications are solved because the IPv6 header improves packet handling by intermediate routers.​

Any host or user can get a public IPv6 network address because the number of available IPv6 addresses is extremely large.​
```
Any host or user can get a public IPv6 network address because the number of available IPv6 addresses is extremely large.​

```
A company designs its network so that the PCs in the internal network are assigned IP addresses from DHCP servers, and the packets that are sent to the Internet are translated through a NAT-enabled router. What type of NAT enables the router to populate the translation table from a pool of unique public addresses, as the PCs send packets through the router to the Internet?

Maximum Attempts: 1

Select one from the below choices:
Port forwarding
Static NAT
PAT
Dynamic NAT
```
Dynamic NAT

```
When dynamic NAT without overloading is being used, what happens if seven users attempt to access a public server on the Internet when only six addresses are available in the NAT pool?

Maximum Attempts: 1

Select one from the below choices:
The first user gets disconnected when the seventh user makes the request.

No users can access the server.

All users can access the server.

The request to the server for the seventh user fails.
```

The request to the server for the seventh user fails.

```
Which of the following describes the difference between AAA authorization and authentication?

Maximum Attempts : 1

Select one from the below choices:
Authentication identifies and verifies a user who is attempting to access a network, and authorization governs what users can and cannot do on the network after they are authenticated

Authentication is a way to audit what actions they performed while accessing the network, and authorization is a way to control who is permitted to access a network

Authentication collects and reports usage data, and authorization keeps a detailed log of exactly what the authenticated user does on the device

Authentication is a way to control what users can and cannot do on the network, and authorization is a way to audit what actions they performed while accessing the network
```
Authentication identifies and verifies a user who is attempting to access a network, and authorization governs what users can and cannot do on the network after they are authenticated

```
Which combination of channel-group modes will establish an EtherChannel by LACP?

Maximum Attempts : 1

Select one from the below choices:
Switch 1 set to Auto; switch 2 set to On
Switch 1 set to Passive; switch 2 set to Active
Switch 1 set to Passive; switch 2 set to Auto
Switch 1 set to Auto; switch 2 set to Desirable

```
Switch 1 set to Passive; switch 2 set to Active

```
In heavily populated wireless networks, which of the following would provide faster data transmission for wireless clients?

Maximum Attempts : 1

Select one from the below choices:
Configuring wireless clients to preferentially use 2.4GHz and selecting 2, 7, and 12 channels
Configuring wireless clients to preferentially use 5GHz because of the large amount of non-overlapping wireless channels
Add more Wireless LAN Controller and select the same channel
Configuring wireless clients to preferentially use 2.4GHz because of the large amount of non-overlapping wireless channels
```
Configuring wireless clients to preferentially use 5GHz because of the large amount of non-overlapping wireless channels

```
When a router has learned the same route entry from two different routing protocols, which one will be installed?

Maximum Attempts : 1

Select one from the below choices:
The route with the shortest prefix length
The route with the lowest administrative distance
The route with the longest prefix length
The route with the lowest metric
```

The route with the lowest administrative distance
```
Which of the following is NOT an advantages of EtherChannel?

Maximum Attempts : 1

Select one from the below choices:
The loss of one physical link within the channel does not create a change in the topology, and the EtherChannel remains functional

EtherChannel provides redundancy because the overall link is seen as one logical connection

Gigabit Ethernet and 10 Gigabit Ethernet cannot be mixed within a single EtherChannel

Most configuration tasks can be done on the EtherChannel interface instead of on each individual port, ensuring configuration consistency throughout the links
```

Gigabit Ethernet and 10 Gigabit Ethernet cannot be mixed within a single EtherChannel

```
Which of the following descriptions about RSTP is correct?

Maximum Attempts : 1

Select one from the below choices:
RSTP can achieve much faster convergence than legacy STP when the Layer 2 network topology changes
It increases the size of the broadcast domain
Not only can RSTP allow for redundancy while creating a loop-free Layer 2 topology, but also it improves switch performance
RSTP can be used on Layer 3 switches, while STP can only run on Layer 2 switches
```
RSTP can achieve much faster convergence than legacy STP when the Layer 2 network topology changes
```
In which case should router-on-a-stick be configured?

Maximum Attempts : 1

Select one from the below choices:
When native VLAN need to be configured on a Layer 3 switch
Router-on-a-stick is only used when routers adopt a static routing scheme
Router-on-a-stick is only used when only one router in a network
Using existing routers without occupying multiple physical interfaces for inter-VLAN routing
```

Using existing routers without occupying multiple physical interfaces for inter-VLAN routing

```
Which of the following correctly describes FlexConnect AP?

Maximum Attempts : 1

Select one from the below choices:
Passive mode and Active mode are two modes of operation for the FlexConnect AP
In the standalone mode, the FlexConnect AP has CAPWAP connectivity with its WLC and can send traffic through the CAPWAP tunnel
In the standalone mode, a FlexConnect AP can assume some of the WLC functions such as switching client data traffic locally and performing client authentication locally
In connected mode, a FlexConnect AP can assume some of the WLC functions such as switching client data traffic locally and performing client authentication locally
```
In the standalone mode, a FlexConnect AP can assume some of the WLC functions such as switching client data traffic locally and performing client authentication locally

```
A company has been assigned the 203.0.113.0/27 block of IP addresses by the ISP. The company has over 6000 internal devices. What type of NAT would be most appropriate for the employee workstations of the company?

Maximum Attempts: 1

Select one from the below choices:
Static NAT
Dynamic NAT
Dynamic NAT overload using the pool of addresses
TCP NAT
```

Dynamic NAT overload using the pool of addresses
```
What is the recommended technology to use over a public WAN infrastructure when a branch office is connected to the corporate site?

Maximum Attempts: 1

Select one from the below choices:
ATM
ISDN
VPN
X.25
```
VPN
```
Which medium do service providers use to transmit data over WAN connections with SONET, SDH, and DWDM?

Maximum Attempts: 1

Select one from the below choices:
Fibre Optic
Satellite
Copper
Wi-Fi
```
Fibre Optic
```
A small company with 10 employees uses a single LAN to share information between computers. Which type of connection to the Internet would be appropriate for this company?

Maximum Attempts: 1

Select one from the below choices:
A dialup connection that is supplied by their local telephone service provider
Virtual Private Networks that would enable the company to connect easily and securely with employees
A broadband service, such as DSL, through their local service provider
Private dedicated lines through their local service provider
```
A broadband service, such as DSL, through their local service provider
```
The use of 3DES within the IPsec framework is an example of which of the IPsec building blocks?

Maximum Attempts: 1

Select one from the below choices:
Confidentiality
Authentication
Non- Repudiation
Intergrity
```
Confidentiality
```
Which technique is necessary to ensure a private transfer of data using a VPN?

Maximum Attempts: 1

Select one from the below choices:
Encryption
Authentication
Sensitivity
Authorization
```
Encryption
```
Which VPN type is a service provider managed VPN?

Maximum Attempts: 1

Select one from the below choices:
Remote-Acesss VPN
SSL
MPLS VPN
IPSEC
```
MPLS VPN
```
Which IPSEC framework protocol provides data integrity and data authentication, but does not provide data confidentiality?

Maximum Attempts: 1

Select one from the below choices:
AH
ESP
3DES
DH3

```
AH
```
A network engineer performs a ping test and receives a value that shows the time it takes for a packet to travel from a source to a destination device and return. Which term describes the value?

Maximum Attempts: 1

Select one from the below choices:
Jitter
Latency
Bandwidth
Priority
```
Latency

```
Which device would be classified as a trusted endpoint?

Maximum Attempts: 1

Select one from the below choices:
Switch
IP Phone
Firewall
Router
```
IP Phone
